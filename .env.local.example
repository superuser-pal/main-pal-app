# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Stripe Configuration
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# PromptPal API Key Encryption
# Generate with: openssl rand -hex 32
# Production: Store in Supabase Vault instead (see INSTALL.md Phase 3)
API_KEY_ENCRYPTION_SECRET=

# Optional: Server-side LLM Provider Keys (for testing/demo only)
# Users should provide their own keys via the encrypted_api_keys table
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: Extension API Rate Limiting (Upstash Redis)
# Required for production extension API (see INSTALL.md Phase 3.2)
# Sign up at https://upstash.com and create a Redis database
# UPSTASH_REDIS_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_TOKEN=